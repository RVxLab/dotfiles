# Dotfiles

My dotfiles and system installation scripts for MacOS

## Installation

Clone this repo anywhere

```shell
git clone git@github.com:/RVxLab/dotfiles ~/.dotfiles
```

Run the main script

```shell
cd ~/.dotfiles && bash main.sh
```

Each script can also be run on its own
