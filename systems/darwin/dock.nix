{...}: {
  system.defaults.dock = {
    # Automatically hide dock when not hovered
    autohide = true;

    # Don't rearrange spaces based on recent use. Required for Yabai
    mru-spaces = false;
  };
}
